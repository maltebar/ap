<%= form_for(@code_solution_attachment) do |f| %>
  <% if @code_solution_attachment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@code_solution_attachment.errors.count, "error") %> prohibited this code_solution_attachment from being saved:</h2>

      <ul>
      <% @code_solution_attachment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
