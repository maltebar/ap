<h1>List of Submissions</h1>
<table class="table table-bordered">
	<tbody>
		<%CodeSolution.where(code_case_id: @code_case.id).each do |sol|%>
			<tr>
				<td><%=sol.user.name%></td>
				<td><%= link_to "Show", sol%></td>
			</tr>
		<%end%>
	</tbody>
</table>