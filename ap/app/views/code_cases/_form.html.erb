<%= form_for(@code_case) do |f| %>
  <% if @code_case.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@code_case.errors.count, "error") %> prohibited this code_case from being saved:</h2>

      <ul>
      <% @code_case.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
