<%= form_for(@board_post_upvote) do |f| %>
  <% if @board_post_upvote.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@board_post_upvote.errors.count, "error") %> prohibited this board_post_upvote from being saved:</h2>

      <ul>
      <% @board_post_upvote.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
