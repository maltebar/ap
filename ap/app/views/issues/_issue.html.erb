

<%@issues = Issue.where(project_id: @project.id).order('created_at DESC').order('complete DESC')%>
<% @issues.each do |iss|%>
	<%= div_for iss do%>
		<p><%= iss.content %></p>
		<p>Posted <%= time_ago_in_words(iss.created_at) %> ago by <%=iss.user.nickname%></p>
	<%end%>
<%end%>
