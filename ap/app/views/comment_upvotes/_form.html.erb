<%= form_for(@comment_upvote) do |f| %>
  <% if @comment_upvote.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@comment_upvote.errors.count, "error") %> prohibited this comment_upvote from being saved:</h2>

      <ul>
      <% @comment_upvote.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
