<%= form_for(@design_solution) do |f| %>
  <% if @design_solution.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@design_solution.errors.count, "error") %> prohibited this design_solution from being saved:</h2>

      <ul>
      <% @design_solution.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
